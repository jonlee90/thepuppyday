# Task Completion Checklist

After completing code changes:
1. Run `npm run build` to verify compilation
2. Run `npm run lint` to check for lint errors
3. Update affected architecture docs if DB/routes/components changed
4. For DB/schema changes: run Supabase security and performance advisors
5. For component changes: verify no broken references
